{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/db exec drizzle-kit push --config ./drizzle.config.ts && pnpm run build",
  "outputDirectory": "artifacts/crm/dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.mjs"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
